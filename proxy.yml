config:
    target: 'http://localhost:3001/1'
    phases:
    - duration: 60
      arrivalRate: 20
      rampTo: 1000
scenarios:
    - name: "get requests for proxy server"
      flow:
      - get:
          url: "/"